module clk_divider1(
    input clk,
    output reg slow_clk = 0
);
    reg [25:0] count = 0;

    always @(posedge clk) begin
        count <= count + 1;
        if (count == 50000000) begin
            slow_clk <= ~slow_clk;
            count <= 0;
        end
    end
endmodule
